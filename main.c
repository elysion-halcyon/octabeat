#include "Game.h"
void main(void){
	Game game;
	Game_ctor(&game);
	Game_run(&game);
}

///////////////////////////////////////////////////////////////////////////////////////
//変数宣言を一番上へ

//使えない関数がある
//strnicmp(string.h)   (command)    ->for回す
//atof(stdlib.h)   (tempo)　　　　　　　->atoiで済ます %d.%d的なことして再構築？
//realloc(stdlib.h)   (data)      ->最初でメモリ確保してしまう

//Timerを拡張しないとダメ →した　でも正直やりすぎてる感あるのでもし重ければ軽くする
///////////////////////////////////////////////////////////////////////////////////////
